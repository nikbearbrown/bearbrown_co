/**
 * MOVED: the artifact ingest is now scripts/ingest-artifact-pages.py, which
 * sources the ORIGINAL published pages (../nikbearbrown_com/public/artifacts)
 * and preserves their full structure. This stub remains so old instructions
 * fail loudly instead of silently regenerating the flattened version.
 */
console.error('Use: python3 scripts/ingest-artifact-pages.py')
process.exit(1)
